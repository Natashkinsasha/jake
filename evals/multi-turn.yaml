description: "Jake English Tutor — Multi-turn Conversation Eval"

prompts:
  - label: multi-turn-chat
    raw: file://prompts/multi-turn.json

providers:
  - id: anthropic:messages:claude-sonnet-4-20250514
    config:
      max_tokens: 512

defaultTest:
  options:
    provider: anthropic:messages:claude-sonnet-4-20250514
    transform: "output.replace(/^```(?:json)?\\n?/gm, '').replace(/^```$/gm, '').trim()"

tests:
  - file://datasets/multi-turn-cases.yaml
