{
  "name": "@jake/evals",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate-prompts": "tsx scripts/generate-prompts.ts",
    "eval": "tsx scripts/generate-prompts.ts && promptfoo eval $([ -f ../.env ] && echo '--env-file ../.env')",
    "eval:multi": "tsx scripts/generate-prompts.ts && promptfoo eval -c multi-turn.yaml $([ -f ../.env ] && echo '--env-file ../.env')",
    "eval:onboarding": "tsx scripts/generate-prompts.ts && promptfoo eval -c onboarding.yaml $([ -f ../.env ] && echo '--env-file ../.env')",
    "eval:view": "promptfoo view"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "promptfoo": "^0.120.26",
    "tsx": "^4.19.4"
  }
}
